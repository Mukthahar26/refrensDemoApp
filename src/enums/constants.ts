// App main screen names which we use in navigator routes.
export enum screenNames {
  HOME = 'HOME',
  CHARACTERDETAILS = 'CHARACTERDETAILS',
}

// types of Character status
export enum characterStatus {
  unknown = 'unknown',
  Dead = 'Dead',
  Alive = 'Alive',
}
